import { FieldsmatchDirective } from './fieldsmatch.directive';

describe('FieldsmatchDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldsmatchDirective();
    expect(directive).toBeTruthy();
  });
});
